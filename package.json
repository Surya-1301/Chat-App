{
  "name": "chat-app-monorepo",
  "private": true,
  "scripts": {
    "dev": "concurrently \"cd ./backend && npm run dev\" \"cd ./frontend && EXPO_PUBLIC_API_URL=http://localhost:4000 npx expo start --web -c\""
  },
  "devDependencies": {
    "@types/react": "~19.1.11",
    "concurrently": "^9.2.1",
    "typescript": "~5.8.3"
  },
  "dependencies": {
    "expo": "^53.0.20"
    
  }
}
