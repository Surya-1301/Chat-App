import React from 'react';
import App from './AppWeb';

// Root component is the single entry point for the web app.
// Keeping it minimal to let AppWeb provide layout and routing.

export default function Root() {
  return <App />;
}
